import { GrsModel } from '../src/plugin/GrsModel/GrsModel';

describe('GrsModel.getOptions:', () => {
  test('should been defined', () => {
    const model = new GrsModel();
    expect(.getOptions().toBeDefined();
  });
});